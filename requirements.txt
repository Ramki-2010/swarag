# Swarag v1.3 -- Python Dependencies
# Install: pip install -r requirements.txt

# Core pipeline
numpy>=1.23
librosa>=0.10.0
scipy>=1.10
soundfile>=0.12

# Utilities
tqdm>=4.65
pandas>=2.0
matplotlib>=3.7

# Optional: Saraga dataset loader (for metadata cross-referencing)
# mirdata>=1.0

# Optional: Vocal isolation (runs in separate venv: demucs_env/)
# demucs>=4.0
# torch>=2.0
